{
  "name": "autoassist---car-towing-&-automotive-hub",
  "private": true,
  "version": "2.0.0",
  "engines": {
    "node": ">=18.0.0 <=20.x.x",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "dev:backend": "cd server && npm run dev",
    "dev:mobile": "cd mobile && npx expo start --lan",
    "dev:mobile:check": "cd mobile && npm run check:lan",
    "dev:mobile:usb-android": "cd mobile && npm run start:usb-android",
    "dev:mobile:tunnel": "cd mobile && npx expo start --tunnel",
    "mobile:expo": "cd mobile && npx expo",
    "dev:all": "concurrently -n \"BACKEND,MOBILE\" -c \"cyan,magenta\" \"npm run dev:backend\" \"npm run dev:mobile\"",
    "install:all": "npm install && cd server && npm install && cd ../mobile && npm install",
    "build:backend": "cd server && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
